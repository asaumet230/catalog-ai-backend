/**
 * Interface for Server API paths configuration
 * Defines all available API endpoints
 */
export interface PathsProps {
    auth        : string;
    users       : string;
    catalogs    : string;
}
